var { Given, When, Then, DataTable } = require('@badeball/cypress-cucumber-preprocessor');

Given(`I go to SauceDemo`, () => {
    // [Given] Sets up the initial state of the system.
});

When(`I put my {string} and {string}`, (arg0, arg1) => {
    // [When] Describes the action or event that triggers the scenario.
});

Then(`I am connected to SauceDemo`, () => {
    // [Then] Describes the expected outcome or result of the scenario.
});

Then(`I get an error message`, () => {
    // [Then] Describes the expected outcome or result of the scenario.
});

When(`I am connected to SauceDemo`, () => {
    // [When] Describes the action or event that triggers the scenario.
});

When(`I click on Logout`, () => {
    // [When] Describes the action or event that triggers the scenario.
});

Then(`I should be logout`, () => {
    // [Then] Describes the expected outcome or result of the scenario.
});